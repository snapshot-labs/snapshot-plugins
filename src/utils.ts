import snapshot from '@snapshot-labs/snapshot.js';

export const {
  multicall,
  Multicaller,
  subgraphRequest,
  ipfsGet,
  call,
  getBlockNumber,
  getProvider,
  sendTransaction
} = snapshot.utils;

export default {
  multicall,
  Multicaller,
  subgraphRequest,
  ipfsGet,
  call,
  getBlockNumber,
  getProvider,
  sendTransaction
};
