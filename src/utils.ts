import snapshot from '@snapshot-labs/snapshot.js';

export const {
  multicall,
  Multicaller,
  subgraphRequest,
  call,
  getProvider,
  sendTransaction
} = snapshot.utils;

export default {
  multicall,
  Multicaller,
  subgraphRequest,
  call,
  getProvider,
  sendTransaction
};
